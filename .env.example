# Sao chép file này thành .env
# Khi dev dùng Vite proxy: để rỗng (request /api đi qua proxy trong vite.config.js)
# Khi build production: đặt thành URL backend thật, vd https://api.tendomain.com
VITE_API_URL=
